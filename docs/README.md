# Docs

What the code cannot say about itself: the contract between the two halves, how they fit, and the
SEO rules the site has already paid for once. Nothing here restates what the code plainly shows.

| If you are about to… | Read |
| --- | --- |
| Change an endpoint, a response field, or the frontend's API types | [`api-contract.md`](api-contract.md) |
| Change caching, the refresh lock, repo classification or deployment | [`architecture.md`](architecture.md) |
| Touch `frontend/server.ts`, `src/lib/meta.ts`, `public/robots.txt`, `public/llms.txt` or the JSON-LD | [`seo.md`](seo.md) |

A change to any of these behaviours updates the matching document in the same change.
